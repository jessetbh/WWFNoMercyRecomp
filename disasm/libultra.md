# libultra identification evidence — WWF No Mercy

How every named function in `tools/gen_symbols.py`'s `RENAME` map was identified.
Convention inherited from the sister projects (see World Tour's
`disasm/libultra.md` for the original per-function evidence, and WM2000's for the
fingerprint-transfer method this project will use).

## Primary method (planned): fingerprint transfer from WM2000

No Mercy (Nov 2000) is WM2000's direct sequel by the same AKI team, one year
apart — the same relationship as Revenge→WM2000, whose masked full-body
fingerprint transfer matched 27/39. Adapt WM2000's `tools/recon2.py` to match
WM2000's named set against this ROM's fixed segment; record per-function
evidence here as identifications land.

*Empty until recon — nothing has been identified yet.*
